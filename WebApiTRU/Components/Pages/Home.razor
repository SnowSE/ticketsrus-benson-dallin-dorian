@using LibraryTRU
@rendermode InteractiveServer

@page "/"

<HomeComponent></HomeComponent>

<button class="btn btn-warning" @onclick="BuyTicket">BUY A TICKET</button>


@code {

  protected override void OnInitialized()
  {
    TestActivity();
  }
  void TestActivity()
  {
      using var myActivity = Telemetry.MyActivitySource.StartActivity("SayHello");
  }

  void BuyTicket()
  {
    DorianMetric.DorianCounter.Add(1);
    TicketGettingMeterClass.counter.Add(1);
  }
}